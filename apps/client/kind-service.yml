apiVersion: v1
kind: Service
metadata:
  name: asp-invoices-client
spec:
  type: NodePort
  selector:
    app: asp-invoices-client
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3001
      nodePort: 30081
