apiVersion: v1
kind: Service
metadata:
  name: asp-invoices-server
spec:
  type: NodePort
  selector:
    app: asp-invoices-server
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30080
